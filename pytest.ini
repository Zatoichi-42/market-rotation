[pytest]
testpaths = tests
markers =
    unit: Unit tests with synthetic data (fast, deterministic)
    integration: Integration tests with live data (slower, network-dependent)
    slow: Tests that take >5 seconds
addopts = -v --tb=short
